{
  "name": "homeassistant-dashboards",
  "private": true,
  "description": "Tooling only. The strategies ship unbundled and need no build.",
  "scripts": {
    "check": "node --check josa-dashboards.js"
  }
}
